<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:aop="http://www.springframework.org/schema/aop" xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
            http://www.springframework.org/schema/tx
            http://www.springframework.org/schema/tx/spring-tx-4.1.xsd
            http://www.springframework.org/schema/aop
            http://www.springframework.org/schema/aop/spring-aop-4.1.xsd
            http://www.springframework.org/schema/context
            http://www.springframework.org/schema/context/spring-context-4.1.xsd">
    
    <bean id="redeemerConfiguration" class="com.active.services.redeemer.config.RedeemerConfiguration" >
    	<constructor-arg name="autoSync" value="${redeemer.autoSync}" />
    	<constructor-arg name="mongoDBHostAndPortList" value="${redeemer.mongoAddresses}" />
    	<constructor-arg name="mongoUser" value="${redeemer.mongoUser}" />
    	<constructor-arg name="mongoPassword" value="${redeemer.mongoPassword}" />
    </bean>
    
    <bean id="redeemerBootstrap" class="com.active.services.redeemer.core.Bootstrap" />
    
    <bean id="redeemerOperator" class="com.active.services.redeemer.jmx.RedeemerMBean" />
    
    <bean id="redeemerConfig" class="com.active.services.redeemer.core.Configuration" >
    	<property name="mongoDBAdresses" value="${redeemer.mongoDBAdresses}"/>
    	<property name="mongoUser" value="${redeemer.mongoUser}"/>
    	<property name="mongoPassword" value="${redeemer.mongoPassword}"/>
    	<property name="syncThreadPoolSize" value="${redeemer.syncThreadPoolSize}"/>
    	<property name="syncIntervalSec" value="${redeemer.syncIntervalSec}"/>
    	<property name="cleanIntervalSec" value="${redeemer.cleanIntervalSec}"/>
    </bean>
    
 </beans>